# Examples

Well-formed `motion.md` files. Format-by-example is the fastest way to learn the format — read these alongside `../spec/tokens.md` and `../AUTHORING.md`.

> **Placeholder.** No examples yet — the token vocabulary is still settling (the `spring` type is the open decision; see the repo roadmap). First examples land once the schema firms, sourced from a real motion tool (not hand-waved), so they demonstrate real values.

Planned:
- `minimal.motion.md` — the smallest conformant file (a few primitives + one principle).
- `with-springs.motion.md` — springs as params + derived curve.
- `with-sequences.motion.md` — a multi-keyframe named pattern.
